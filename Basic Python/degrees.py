import math

angle_deg = float(input("enter angle in degree:"))
angle_rad = math.radians(angle_deg)
print("cosine=", math.cos(angle_rad))
print("sine=", math.sin(angle_rad))
print("Tangent=", math.tan(angle_rad))